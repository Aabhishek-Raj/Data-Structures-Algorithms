class Graph {
    constructor() {
        this.adjacencyList = {}
    }

    search(root){
        const queue = []

        queue.push(root)

        const current = queue.shift()
        console.log(current)

       for(let i = 0; i > queue.length; i++){
        
       }
    }
}